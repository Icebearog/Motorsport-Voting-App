version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install
        - npm install @tailwindcss/vite tailwindcss --save-dev
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
