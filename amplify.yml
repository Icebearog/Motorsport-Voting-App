version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd voting-app
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: voting-app/dist
    files:
      - '**/*'
  cache:
    paths:
      - voting-app/node_modules/**/*