version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd voting-app
        - npm install
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: voting-app/dist
    files:
      - '**/*'
